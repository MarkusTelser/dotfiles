
# A bash alias is a method of supplementing or overriding bash commands 
# with new ones. Bash aliases make it easy for users to customize their 
# experience in a POSIX terminal. User-level aliases can be defined either
# in the .bashrc file or the .bash_aliases file. The .bash_aliases file
# is to group all your aliases into a separate file instead of putting 
# it in the .bashrc file along with other parameters.

alias cp="cp -i"
alias df='df -h'
alias free='free -m'
alias np='nano -w PKGBUILD'
alias more=less
alias ll="ls -l"
alias la="ls -la"
alias v="vim"
alias n="nvim"

alias sd="shutdown now"
alias re="sudo reboot now"
alias c="clear"
alias x="exit"

alias cdir="cd ~/Code"
alias pdir="cd ~/Code/Python"
alias edir="cd ~/Code/ESP32"
alias mdir="cd /run/media/carlos/FILME/Filme"
